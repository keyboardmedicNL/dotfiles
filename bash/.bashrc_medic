# If not running interactively, don't do anything
case $- in
     *i*) ;;
  *) return;;
esac
# don't put duplicate lines or lines starting with space in the history.
# See bash(1) for more options
HISTCONTROL=ignoreboth

# append to the history file, don't overwrite it
shopt -s histappend

# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTSIZE=1000
HISTFILESIZE=2000

# check the window size after each command and, if necessary,
# update the values of LINES and COLUMNS.
shopt -s checkwinsize

# set a fancy prompt (non-color, unless we know we "want" color)
case "$TERM" in
     xterm-color|*-256color) color_prompt=yes;;
esac

# uncomment for a colored prompt, if the terminal has the capability; turned
# off by default to not distract the user: the focus in a terminal window
# should be on the output of commands, not on the prompt
force_color_prompt=yes

if [ -n "$force_color_prompt" ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
        # We have color support; assume it's compliant with Ecma-48
        # (ISO/IEC-6429). (Lack of such support is extremely rare, and such
        # a case would tend to support setf rather than setaf.)
        color_prompt=yes
    else
        color_prompt=
    fi
fi

if [ "$color_prompt" = yes ]; then
#    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w \$\[\033[00m\] '
    PROMPT_COMMAND='PS1_CMD1=$(git branch --show-current 2>/dev/null)'; PS1='\[\e[38;5;208m\]\A\[\e[0m\]:\[\e[38;5;40m\]\u\[\e[0m\]@\[\e[38;5;40m\]\h\[\e[0m\]:\[\e[38;5;75m\]\w\[\e[0m\]:\[\e[91;1m\]${PS1_CMD1}\[\e[0m\]\$ '
else
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
fi
unset color_prompt force_color_prompt

# If this is an xterm set the title to user@host:dir
case "$TERM" in
xterm*|rxvt*)
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
    ;;
*)
    ;;
esac

# colored GCC warnings and errors
export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'

# start tmux automaticly when starting shell
#if [ $TERM != "linux" ]; then
#     if command -v tmux &> /dev/null && [ -z "$TMUX" ]; then
#         tmux attach-session -t default || tmux new-session -s default
#     fi
# fi

# import auto completion
source /etc/bash/bash_completion.sh

# turn on case ignore for autocompletion
bind -s 'set completion-ignore-case on'

#     Complete arguments as if they were commands
#     (eg: `doas emer<tab>` -> `doas emerge`)
#     (eg: `doas dd status=p<tab>` -> `doas dd status=progress`)
#    Complete arguments as if they were directory names (default behavior)
#     (eg: `doas /bi<tab>` -> `doas /bin/`)
# If you have app-shells/bash-completion installed
complete -F _root_command doas
complete -F _root_command sudo

# run fastfetch
fastfetch

# show reports.txt on terminal start
cat /var/report.txt

# aliases universal
alias ll='ls -al'
alias la='ls -A'
alias l='ls -CF'
alias cat='bat'
alias vlci='vlc -I ncurses'
alias notes='vim ~/Desktop/desktop_docs/Notes'
alias i3conf='vim ~/.config/i3/config'
alias bashconf='vim ~/.bashrc_medic'
alias vimconf='vim ~/.vimrc'

# enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    #alias dir='dir --color=auto'
    #alias vdir='vdir --color=auto'

    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

# aliases alpine
alias endcord='cd ~/Desktop/desktop_docs/Coding/endcord && uv run main.py'
alias suspend='/usr/bin/i3lock && doas /usr/sbin/zzz'
alias update='apk update && apk upgrade'

# aliases ssh and wol
alias buzzer4='ssh -X -C pi@192.168.192.20'
alias rover='ssh -X -C keyboardmedic@192.168.192.10'
alias shed='ssh -X -C keyboardmedic@192.168.192.11'
alias wakerover='wakeonlan 24:4B:FE:4A:27:26 || wol wake 24:4B:FE:4A:27:26 '
alias waketruenas='wakeonlan 00:25:90:A9:67:89 || wol wake 00:25:90:A9:67:89'
