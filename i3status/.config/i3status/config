# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 1
}

order += "ipv6"
order += "wireless _first_"
order += "ethernet _first_"
order += "ethernet tun0"
order += "ethernet proton0"
order += "battery 0"
order += "disk /"
order += "disk /mnt/HD\ Seagate 4TB"
order += "cpu_usage"
order += "load"
order += "memory"
order += "volume master"
order += "tztime local"

ipv6 {
        format_up = "IPV6 %ip"
        format_down = ""
}

wireless _first_ {
        format_up = "Wir: (%quality at %essid) %ip"
        format_down = ""
}

ethernet _first_ {
        format_up = "Eth: %ip"
        format_down = "E: down"
}

ethernet tun0 {
        format_up = "OVPN: %ip"
        format_down = ""
}

ethernet proton0 {
        format_up = "PROTON: %ip"
        format_down = "PROTON DOWN"
}

battery 0 {  
    format = "%status %percentage%"
    format_down = "" 
}  

disk "/" {
        format = "Dsk %avail"
}

disk "/mnt/HD\ Seagate\ 4TB" {
        format = "4TB %avail"
}

cpu_usage {
        format = "CPU %usage"
        max_threshold = 80
}

load {
        format = "Lo %1min"
        max_threshold = 10
        format_above_threshold = "Lo %1min"
}

memory {
        format = "Mem %used  %total"
        threshold_degraded = "1G"
        format_degraded = "Mem < %available"
}

volume master {
        format = "Vol %volume"
        format_muted = "muted (%volume)"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}


tztime local {
        format = "%Y-%m-%d %A %H:%M"
}
